name: Deploy test workflow in main

on:
  push:
   branches:
     - test


jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Deploy to relase
        run: echo "Deploying release"
